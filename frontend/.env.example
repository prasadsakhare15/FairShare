# Optional: API base URL (leave empty when using Vite proxy in development)
# Example for production: https://api.yourdomain.com
VITE_API_URL=

# Optional: request timeout in ms (default 15000)
# VITE_API_TIMEOUT=15000
